<android.support.v4.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.centerstage.limelight.MovieFragment"
    android:fillViewport="true"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView android:text="Hello World Collapsing the toolbar can be fine-tuned via app:layout_collapseMode. It accepts three values:  For example, setting this attribute tois useful for applying a nice parallax effect to images.
The CoordinatorLayout deserves a bit more attention, so I’ll return to it in a later post.This blog post wouldn’t be complete without putting these new toys into action. Here’s a demo that shows:llapsing the toolbar can be fine-tuned via app:layout_collapseMode. It accepts three values:  For example, setting this attribute tois useful for applying a nice parallax effect to images.
The CoordinatorLayout deserves a bit more attention, so I’ll return to it llapsing the toolbar can be fine-tuned via app:layout_collapseMode. It accepts three values:  For example, setting this attribute tois useful for applying a nice parallax effect to images.
The CoordinatorLayout deserves a bit more attention, so I’ll return to it llapsing the toolbar can be fine-tuned via app:layout_collapseMode. It accepts three values:  For example, setting this attribute tois useful for applying a nice parallax effect to images.
The CoordinatorLayout deserves a bit more attention, so I’ll return to it llapsing the toolbar can be fine-tuned via app:layout_collapseMode. It accepts three values:  For example, setting this attribute tois useful for applying a nice parallax effect to images.
The CoordinatorLayout deserves a bit more attention, so I’ll return to it so I’ll return to it llapsing the toolbar can be fine-tuned via app:layout_collapseMode. It accepts three values:  For example, setting this attribute tois useful for applying a nice parallax effect to images.
The CoordinatorLayout deserves a bit more attention, so I’ll return to itso I’ll return to it llapsing the toolbar can be fine-tuned via app:layout_collapseMode. It accepts three values:  For example, setting this attribute tois useful for applying a nice parallax effect to images.
The CoordinatorLayout deserves a bit more attention, so I’ll return to it" android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

    </LinearLayout>

</android.support.v4.widget.NestedScrollView>